#pragma once

#include "xutil/prefix.h"

// the http default timeout, 10s
#define XU_HTTP_DEFAULT_TIMEOUT (10000)

// the http default redirect maxn
#define XU_HTTP_DEFAULT_REDIRECT (10)

// the http default port
#define XU_HTTP_DEFAULT_PORT (80)

// the http default port for ssl
#define XU_HTTP_DEFAULT_PORT_SSL (443)
